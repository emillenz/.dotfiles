modmap:
  - name: global
    remap:
      capslock:
        held: ctrl_l
        alone: esc
        one_timeout_milis: 50

keymap:
  - name: browser
    application:
      not: [Emacs-gtk+x11, Emacs, Alacritty]
    remap:
      C-n: down # vim completion bindings
      C-p: up
